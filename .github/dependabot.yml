version: 2
updates:
  # GitHub Actions — keep workflow dependencies (actions/checkout etc.) current
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(ci)"
